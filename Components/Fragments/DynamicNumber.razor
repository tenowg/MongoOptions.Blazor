@typeparam TValue
@using System.Linq.Expressions
@using System.Reflection
@using Microsoft.AspNetCore.Components.Forms
@using MongoOptions.Extensions

<InputNumber TValue="TValue" 
             Value="@((TValue)Property.Getter(Target)!)" 
             ValueExpression="@Expression"
             ValueChanged="@(val => Property.Setter(Target, val))"
             class="form-control" />

@code {
    [Parameter] public PropertyMetadata Property { get; set; } = default!;
    [Parameter] public object Target { get; set; } = default!;
    [Parameter] public Expression<Func<TValue>> Expression { get; set; } = default!;
}